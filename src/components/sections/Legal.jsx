import React from "react";
import LegalIntro from "../renders/LegalIntro.jsx";
const Legalrendering = ({ prop }) => {
  if (prop === "LegalIntro") {
    return <LegalIntro />;
  }
  if (prop === "LegalIntro") {
    return <LegalIntro />;
  }
  if (prop === "LegalIntro") {
    return <LegalIntro />;
  }
  if (prop === "LegalIntro") {
    return <LegalIntro />;
  }
  return null;
};

export default Legalrendering;
